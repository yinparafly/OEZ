# ESP32_AS5047P 转速采样与控制架构需求（可交接总纲）

> **文档位置**：仓库根目录 `E:\OEZCON\ESP32_AS5047P_转速采样与控制架构需求.md`  
> **工程路径**：`E:\OEZCON\mcoder\ESP32_AS5047P`  
> **固件入口**：`firmware/ESP32ReadAS5047P/ESP32ReadAS5047P.ino`（及 `ble_host.*`）  
> **台架**：ESP32-S3 + AS5047P（SPI，GPIO10/11/12/13）+ 航模 ESC（信号 GPIO9）+ 扑翼输出盘霍尔（GPIO4/5）· 串口 COM10@921600  
> **版本**：2026-07-22 · 合并自采样方案、高频改造、晚间工作计划与双核架构建议  
> **约束**：本文档为需求与架构总纲；实施时另开任务。本文**不改固件、不烧录、不跑电机、不 git commit/push**。

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [术语定义](#2-术语定义必须写死)
3. [现状架构](#3-现状架构)
4. [目标架构](#4-目标架构)
5. [通信策略](#5-通信策略测试态--飞行态)
6. [FreeRTOS / Arduino 选型结论](#6-freertos--arduino-选型结论)
7. [节拍对齐](#7-节拍对齐弃-250采-200400-整数系)
8. [实施步骤与验收标准](#8-实施步骤与验收标准)
9. [测试计划衔接](#9-测试计划衔接)
10. [关键路径索引](#10-关键路径索引)
11. [外部评估与反馈（2026-07-22）](#11-外部评估与反馈2026-07-22)

---

## 1. 项目背景与目标

### 1.1 业务场景

- 扑翼机电机转速闭环：编码器测速 + PI/前馈 + ESC 油门输出；霍尔盘做独立交叉验证。
- **有效工况 ≥2000RPM**（低速无气动意义，且易顿挫）；量程目标放宽至 **12000RPM**。
- 电调刷新目标固定为 **400Hz**；瞬时速度需高精度、低延迟、抗噪，以应对扑翼 8–12Hz 周期负载波动。

### 1.2 已解决的核心矛盾（2026-07-21）

| 问题 | 原状 | 已落地 |
|------|------|--------|
| 测速折叠 | 测速绑在 250Hz 控制环，Nyquist 仅 **7500RPM** | 编码器采样/测速解耦到 **2kHz esp_timer**，解缠 Nyquist **60000RPM** |
| 转速正确性 | 负载变更后需复核 | 编码器与展开角一致；霍尔定点减速比 ≈ **27.744**（与设计偏差 ~0） |
| 闭环品质 | 旧前馈偏高、超调大 | 重学前馈 + PID/GainMap；2000–6000RPM 超调 ≤2.3% |
| 高速阶梯 | — | **7000RPM 首档已达成**（均值 ~6997）；用户要求停止，未做 +500 |

### 1.3 本轮架构改造目标（待实施）

口号：**高频采集/使用，低频输出**（采密、控疏）。  
（旧称「采样/取用 / 控制输出」已废；见 §2。）

| 目标项 | 数值 / 要求 |
|--------|-------------|
| 采集 / 使用 | **保持 2000Hz**（采 2k、用 2k；方案下限 ≥1200Hz；不降到 400） |
| 输出数据频率 | **250 → 400Hz**，PID→ESC，与 ESC **FREQ 400** 同相 |
| 系统节拍（可选） | **200Hz**：状态机 / 看门狗 / 日志——不跑 PID、不写 ESC |
| 任务形态 | 多频率独立任务 + Core 分工；弃「单 loop 嵌套 250」 |
| 通信 | 测试：关 WiFi、暂用 BLE；飞行：关 BLE |
| 框架 | **保持 Arduino-ESP32**（其上用 FreeRTOS API） |

### 1.4 方案原文采样率建议（对照）

来自 `关于转速采样的问题.txt`（面向「高精度瞬时速度 + ESC 400Hz」）：

| 采样率 | 结论 |
|--------|------|
| **1200Hz** | 首选：每 2.5ms 控制窗内约 3 新点，近 6 点可做 LS；延迟≈0.416ms |
| 800Hz | 次选 |
| 400Hz | 不推荐（窗内点太少，差分噪声大） |

**本工程决策**：已验证 **2kHz** 稳定且 CPU≈2.5–3%，**保持 2k**；1200 仅为方案下限，**不为「贴原文」而降采样**。

---

## 2. 术语定义（必须写死）

> **正式术语（用户规定）**：**采集数据 / 使用数据 / 输出数据频率**（简称：采集 / 使用 / 输出）。  
> **「取用」作废**：旧文若出现「取用」，一律按「使用」理解，或已改为本表用语；**禁止**再把 400Hz 叫「取用」或「使用」。  
> **禁止**把「采集 / 使用」与「输出」混称。

| 术语 | 含义（写死） | 现状 | 目标 | 禁止 |
|------|--------------|------|------|------|
| **采集**（采集数据） | **2kHz SPI** 读角 → 入缓冲或快照 | **2000Hz**（esp_timer） | **2000Hz**（或 ≥1200） | — |
| **使用**（使用数据） | **2kHz 求速**（解缠、窗差分/拟合等）；**采 2k 用 2k**，与采集同频或同层 | **≈2000Hz**（回调内测速） | **同采集层 ≈2k（或 1200）** | **禁止把 400Hz 叫「使用」**（旧称「取用」同禁） |
| **输出**（输出数据频率） | **400Hz PID→ESC**（斜坡/写 PWM）的节拍 | **250Hz** | **400Hz** | 勿与采集/使用混称 |
| **ESC 帧率** | 电调硬件 PWM/DShot 刷新 | 默认 **400**（可 `FREQ`） | **FREQ 400** 与输出同相 | — |
| **系统节拍** | 状态/看门狗/低频日志 | 无独立 | 可选 **200Hz** | 不是使用、不是输出 |
| **USB 遥测 Hz** | **完整遥测帧率**＝每秒 **N 行**完整 CSV（1 帧＝1 行全字段） | 随控制拍 ≈**250** 行/秒 | 默认 **20** 帧/秒 | **不是**帧内计算次数；与采集2k/使用2k/输出400无关，遥测只读快照 |

**写死数据流**：

```
采集2k → 使用2k(求速全用) → 输出400(最新RPM→ESC)
```

（展开：采集 @2000Hz SPI → 使用 @2000Hz 求速（采2k用2k）→ 输出 @400Hz 读最新 ω → PID → ESC @400Hz）

每输出拍**消费最新使用结果**（最新滤波 RPM，或近 N 点拟合），**不是**把 400Hz 当成使用频率。

### 2.1 易错说法对照

| 说法 | 对错 |
|------|------|
| 「现在采集真的是 2k（SPI）」 | ✅ |
| 「现在使用（求速）也是 ≈2k」 | ✅（采 2k 用 2k；在采集回调里算速） |
| 「现在输出也是 2k」 | ❌（输出现状 **250Hz**，目标 **400Hz**） |
| 「取用/使用只有 400Hz」 | ❌（把输出误叫成使用；旧「取用」已废；**400 是目标输出**） |
| 「2k 采集/使用 + 低频输出」 | ✅（目标输出改为 **400**，弃 250） |
| 「USB 遥测 100Hz / 20Hz」 | ✅ **Hz＝完整帧率**（行/秒）；旧稿 100＝100 帧/秒，现定 **20 帧/秒**；≠帧内算速；≠采集/使用/输出 |

### 2.2 「1200 / 400 / 6 点 / 2400」语义（勿混账）

| 说法 | 正确含义 |
|------|----------|
| 采样 1200 | 每秒 **1200** 个唯一点（SPI/角度） |
| 相邻差分 | 稳态下差分次数 ≈1200；「2400」= **1200×2 端点引用**，**不是** 2400 个唯一点 |
| 6 点 LS（方案推荐） | 在 **400Hz 控制拍**上取环缓近 **6** 点做最小二乘 → 本拍一个 ω |
| 1200÷400 | = **3**：每控制拍约 **3** 个新采样点 |
| 本工程现状 | 2k 回调内 **8 点窗差分 + EMA**；控制拍只读最新滤波 RPM |

---

## 3. 现状架构

### 3.1 频率总表

| 链路 | 数值 | 角色 |
|------|------|------|
| 编码器采集（SPI + 解缠） | **2000Hz**，周期 500µs，`esp_timer`（`ESP_TIMER_TASK`） | **采集** |
| 测速更新（窗差分 + EMA） | **≈2000Hz**（与采集同回调；窗 8 样本 = 4ms；采 2k 用 2k） | **使用**（旧称「取用」已废） |
| 控制（PID / 斜坡 / `applyEscPulse`） | **250Hz**（`loop` + `millis` 嵌套） | **输出**（目标 400Hz） |
| ESC PWM | 默认 **50Hz**；`FREQ 50..600` | 执行器（未强制与输出对齐） |
| USB 遥测 | 现状随控制拍 ≈**250** 行/秒；**目标默认 20Hz**（20 帧/秒完整 CSV；见 §2 / §10.5） | 通信 |
| BLE 遥测 | 默认 **20Hz** 瘦身行（可调 5–50） | 通信 |
| WiFi / OTA | **未使用** | — |
| 霍尔 | `gpio_isr` ANYEDGE + 软件消抖；主循环 `hallPoll` | 事件驱动 |

### 3.2 编码器与测速（已验证）

- SPI **8MHz**；回调内：ANGLECOM → ±8192 解缠累加 `int64 counts` → 窗差分 → 尖峰拒绝 → EMA 0.9/0.1 → 符号锁定（~83ms）→ `portMUX` 写 `EncShared`。
- 共享：`portMUX` 整块快照；**无** ring + semaphore。
- 实测：`enc_sample_hz=2000` 稳定；采样 CPU **≈2.5–3%**；主循环空转 ~7万–10万 Hz。
- 交叉验证：2000/4000/6000RPM 与霍尔误差 **0.0 / 0.6 / 0.1%**；越过旧 7500 折叠界（~7300 仍吻合 0.3%）。空载撞动力/前馈饱和约 **~7300RPM**，**不是测速上限**。

### 3.3 任务与双核（实际 ≈ 默认调度）

- **无**应用层 `xTaskCreate` / `xTaskCreatePinnedToCore`。
- 应用逻辑几乎全在 Arduino **`loopTask` → `setup()`/`loop()`**（默认 **Core1**）。
- 编码器在 **esp_timer 系统任务**回调中执行（**不绑定核心**；实际核随 IDF 调度，可能迁移——勿写死「常见 Core0」）。
- BLE 栈（Bluedroid/NimBLE）自建任务，通常偏 **Core0**，与 timer/SPI **潜在争用**，未主动管理。

```
Core0（协议栈/系统，未 pin）              Core1（Arduino loopTask，默认）
┌──────────────────────────────┐        ┌──────────────────────────────┐
│ BT/BLE 栈（测试态开）         │        │ loop：串口/BLE 命令轮询        │
│ esp_timer → encSampleCb      │        │ 250Hz：快照/PI/ESC（现状）     │
│ （SPI、解缠、窗口测速；核不定） │        │ USB 遥测目标 20Hz；BLE lite    │
│ WiFi：未用                    │        │ hallPoll / phaseTick          │
│ GPIO ISR：霍尔                │        │                              │
└──────────────────────────────┘        └──────────────────────────────┘
         │ portMUX 快照                          │
         └────────────── EncShared ──────────────┘
```

### 3.4 与方案建议的差距（摘要）

| 建议项 | 现状 | 判定 |
|--------|------|------|
| 采样 ≥1200Hz（硬定时器） | 2kHz esp_timer | ✅ 已做且更优 |
| 禁止 vTaskDelay 高频采样 | 未用 delay 采样 | ✅ |
| 回调极简 + semaphore | 回调偏重；portMUX 轮询 | ⚠️ / ❌ 可选增强 |
| 控制 400 + ESC 400 | CTRL=250，ESC 默认 50 | ❌ 未对齐 |
| 控制侧 6 点 LS | 采样侧 8 点窗+EMA | ⚠️ 效果同类，算法不同 |
| 显式双核 pin | 无 | ❌ |
| 飞行关 BLE | 上电即 advertising | ❌ |

---

## 4. 目标架构

### 4.1 目标频率表（整数系）

| 链路 | 频率 | 角色 | 与 400Hz 输出 | 与 200Hz 节拍 |
|------|------|------|---------------|---------------|
| 编码器采集（SPI） | **2000**（或 **1200**） | **采集** | 5× / 3× | 10× / 6× |
| 测速/差分/拟合 | **同采集（采 2k 用 2k）** | **使用** | 每拍读最新结果 | 同上 |
| PID→写 ESC | **400** | **输出** | 1× | 2× |
| ESC PWM 帧 | **400**（`FREQ 400`） | 执行器 | **同相 1:1** | 2× |
| 状态/看门狗/日志 | **200**（可选） | 系统节拍 | 1/2 | 1× |
| USB 遥测 | **20**（默认完整帧率） | 通信 | 远低于输出；**只读快照** | — |
| BLE 遥测（仅测试） | **20** | 通信 | 远低于输出 | — |

> **USB 遥测 Hz 写死**：**Hz = 完整遥测帧率**（每秒 N 行 CSV），不是帧内计算次数。原先草案「**100Hz**」＝100 帧/秒；现用户定 **20 帧/秒**。与采集2k / 使用2k / 输出400 **无关**——遥测任务只读快照并打印。

**刻意不做**：把输出升到 2kHz——电调只需 ~400Hz；升到 2k 浪费 CPU、放大 PWM 改写抖动，违背「高频采集/使用、低频输出」。

### 4.2 多任务分频（目标）

| 任务 | 频率 | 核 | 优先级 | 实现要点 |
|------|------|----|--------|----------|
| 编码器采样 | **2000Hz** | **不绑定核心**（esp_timer 回调，尽量短） | timer 任务级 | 保持 esp_timer；SPI+解缠+写环缓/快照；（可选）测速仍可放此或迁控制侧；**禁止**回调内 `Serial.print`/`printf` |
| 电机控制 | **400Hz** | **Core1** | 最高应用优（如 4–5） | `xTaskCreatePinnedToCore`；**推荐** esp_timer 触发信号量唤醒（µs 级）；简易可用 `vTaskDelayUntil` 但默认 tick=1ms 时 `pdMS_TO_TICKS(2.5)` 会落成 2 或 3ms（~±1ms 抖动）——见 §6.1；读最新 RPM（或近 6 点 LS）→ PID → ESC |
| ESC 帧 | **400Hz** | LEDC/DShot 硬件 | — | `FREQ 400`；与输出同相 |
| USB 命令+遥测 | 命令尽量泵；**USB 遥测默认 20Hz**（20 完整帧/秒；旧稿「100Hz」＝100 帧/秒，现定 20） | Core0 或低优 Core1 | 低（1–2） | 独立任务或降级 `loop`；只读快照；**禁止**在 2k 回调/400 控制任务内 `Serial`/`hostPrintf` |
| BLE NUS | TX ~20Hz；RX 事件 | Core0（栈已在此） | 低 | **测试 ON / 飞行 OFF** |
| 霍尔 | 边沿事件 | ISR → 控制拍消费 | ISR 最高 | ISR 只记时间戳/计数 |
| sysBeat（可选） | **200Hz** | 任一低负载核 | 低 | 状态/看门狗/日志；**不嵌套电机** |

目标数据流：

```
esp_timer @2000Hz                 ctrlTask @400Hz (Core1, 高优)
  SPI → 解缠 → 快照/环缓    →     取最新 rpm（或 6 点 LS）
                                → PID → applyEscPulse → ESC @400Hz

commTask / loop（低优）           BLE 栈（Core0，仅测试）
  串口命令 + USB 遥测 @20Hz         NUS RX/TX @~20Hz

sysBeat @200Hz（可选）
  状态 / 看门狗 / 低频日志
```

### 4.3 Core 分工原则

1. **Core1 = 控制确定性核**：PID/斜坡、写 ESC、急停、（可选）测速拟合；高优先级、少阻塞 I/O。  
2. **Core0 = 通信与系统核**：BLE（仅测试）、USB 重度打印可缓；WiFi 永不默认开。  
3. **采样路径**：继续 esp_timer（µs 级）；飞行关 BLE 后 Core0 上 timer/SPI 抖动下降。  
4. **霍尔 ISR**：保持极短；逻辑在控制拍消费。

### 4.4 测速算法路径（可选演进）

| 阶段 | 算法位置 | 说明 |
|------|----------|------|
| 现状 / 阶段 B 默认 | 采样回调：8 点窗差分 + EMA | 已验证；控制拍只取最新 \|rpm\| |
| 可选增强（阶段 D） | 控制侧：近 6 点最小二乘 | 对应方案文「甜点」；2k 下 6 点≈3ms 窗，与现 8 点/4ms 接近 |
| 回调瘦身（可选） | 回调只写 `(t_us, counts)` 环缓 + 信号量 | **非阻塞项**：预估回调总耗时 <20µs ≪ 500µs，**无需为时序而瘦身**；若上环缓，仍须保证回调远小于周期 |

#### 4.4.1 环形缓冲区（若阶段 D 采用，须写死）

2kHz 采样、400Hz 控制拍 → 每拍约消费 **5** 个新点。

| 项 | 约定 |
|----|------|
| **深度** | **16 或 32**（2 的幂，便于位运算取模） |
| **读写隔离** | 写指针在采样回调递增（`volatile`）；读指针在控制任务递增 |
| **追尾策略** | **写指针永远不追读指针**；若追尾 → 控制消费过慢 → **报警并丢弃旧数据**，**禁止**阻塞采样 |
| **原子性** | 先写完 `(t_us, counts)` 再更新写指针；读侧须读到完整一对 |
| **文件** | `enc_ring.h`：深度、`push`/`pop`/`peek_n`、追尾计数/标志 |

---

## 5. 通信策略（测试态 / 飞行态）

| 项目 | 测试态（台架 / 手机调试） | 飞行态（正式飞行） |
|------|--------------------------|-------------------|
| **WiFi** | **OFF**（固件本未用；保持） | **OFF** |
| **BLE advertising / NUS** | **ON**（暂用） | **OFF**（停 adv、断连、可选 deinit） |
| BLE 遥测/遥控 | ON，限速瘦身（默认 20Hz） | 无 |
| USB 串口 | ON，遥测默认 **20Hz**（完整 CSV 帧率） | 地面可开；空中不依赖 |
| 编码器采样 | 2kHz esp_timer | 同左（更干净） |
| 控制 / ESC | 目标 400 / 400 | 同左 |
| Core0 主负载 | BLE + timer/SPI | **几乎只有 timer/SPI** |
| 主机超时急停 | 保留（防台架失控，1.5s） | **`FLIGHT ON` 时自动禁用主机超时急停**（或放宽至 ≥10s）；`FLIGHT OFF` 恢复 1.5s 台架保护 |

**命令方向（阶段 A）**：`BLE ON` / `BLE OFF` / `BLE?`；可选 `FLIGHT ON`（关 BLE + **禁用/放宽主机超时**）/ `FLIGHT OFF`；编译开关 `FEATURE_BLE_DEFAULT`（测试 1 / 飞行 0）；banner 打印 `mode=TEST|FLIGHT ble=on|off wifi=off`。

**验收硬项（防空中误急停）**：`FLIGHT ON` 后，串口断连 / 主机停发保活 **不得** 触发 ESTOP。

---

## 6. FreeRTOS / Arduino 选型结论

### 6.1 结论（写死）

| 决策 | 内容 |
|------|------|
| **应用框架** | **保持 Arduino-ESP32**（FQBN `esp32:esp32:esp32s3`） |
| **不迁** | 不整体迁移到纯 ESP-IDF 工程（成本高、打断现有串口命令/BLE/台架脚本） |
| **OS API** | 在 Arduino-ESP32 **之上**使用 FreeRTOS：`xTaskCreatePinnedToCore`、`vTaskDelayUntil`、portMUX、信号量等 |
| **高频采样** | **必须 esp_timer**；**禁止**用 `vTaskDelay`/`vTaskDelayUntil` 跑 1200/2000Hz（默认 tick 1ms，调度不准且达不到 2k） |
| **输出 400Hz（PID→ESC）** | **方案一（简易）**：独立任务 + `vTaskDelayUntil`——默认 tick=1ms 时 `pdMS_TO_TICKS(2.5)` 会四舍五入为 **2 或 3**，周期在 2–3ms 间抖（~±1ms）；若抖动可接受（<±0.5ms）可暂用。**方案二（推荐）**：另起 **esp_timer @400Hz** 释放信号量，控制任务阻塞等待 → µs 级节拍 |
| **采样方案选型（已落地）** | esp_timer + 同步 SPI ✅；`vTaskDelayUntil` 任务采样 ✗；SPI-GDMA / ABI+PCNT 暂缓 |
| **数值运算** | 控制/滤波/拟合链路优先 **Q16.16 定点**（见 §11.3）；遥测打印再转浮点 |

### 6.2 理由摘要

- 现有固件、PC 脚本、BLE NUS、命令集均建立在 Arduino-ESP32 上；架构改造是**节拍与任务拆分**，不是换框架。  
- FreeRTOS 多任务分频与 Arduino `loop` 可并存：控制 pin Core1 后，`loop` 可降为通信泵。  
- 2kHz 采样已用 esp_timer 验证；控制升到 400Hz **不必**改系统 tick，但若要精确 2.5ms 周期，应用 **esp_timer 触发** 而非依赖 `vTaskDelayUntil(2.5ms)`。

---

## 7. 节拍对齐（弃 250，采 200/400 整数系）

### 7.1 为何弃 250

| 关系 | 数值 | 问题 |
|------|------|------|
| 400 ÷ 250 | **1.6** | **非整数倍** |
| 同 loop 嵌套 | 4ms 控拍 vs 2.5ms 出拍 | 相位别扭、易抖、难对日志 |

**结论**：弃用 **250Hz** 作为系统/控制基准；**使用**仍保持 2k（或 1200；采 2k 用 2k），只把**输出** 250→400。

### 7.2 干净整数关系

- **200 : 400 = 2 : 1**（系统节拍 : 输出）  
- **2000 : 400 = 5 : 1**；**1200 : 400 = 3 : 1**（采集/使用 : 输出）  
- ESC 首选 **FREQ 400** 与输出同相；若 ESC=200，则每硬件帧内输出改写 2 次、末值生效——不如同 400。

### 7.3 同构口号

| | 采集 / 使用 | 输出（PID→ESC） |
|--|-------------|-----------------|
| 现状 | 2k / 2k | 250 |
| **目标** | 2k / 2k（或 ≥1200） | **400** |
| 可选 | — | 另加 **200Hz** 仅状态/日志 |

**400 = 输出，≠ 使用。**（「取用」作废，勿再用。）

---

## 8. 实施步骤与验收标准

> 执行顺序：**A 通信 → B 控制/ESC 400 → C 双核 pin（按需）→ D 测速/环缓（可选）→ E 定点化（可选）**。  
> 暂缓：SPI-GDMA、ABI+PCNT、大规模重构。

### 阶段 A — 通信模式（优先，改动小）

| 步骤 | 内容 |
|------|------|
| A1 | `ble_host`：`bleEnd()` / `bleSetEnabled(bool)`；关闭时短路 TX/RX |
| A2 | 命令：`BLE ON/OFF/?`；可选 `FLIGHT ON/OFF` |
| A3 | `#define FEATURE_BLE_DEFAULT`；飞行固件默认不 `bleBegin()` |
| A4 | WiFi 保持不用 |
| A5 | Banner：`mode=TEST\|FLIGHT ble=… wifi=off` |

**验收**：

- [x] 上电默认：WiFi 未启动；测试固件 BLE 可连；banner 标明模式（2026-07-22 A/B 烧录）  
- [x] `BLE OFF` / `FLIGHT ON`：命令已实现；`ENC?` meas≈2000  
- [ ] 飞行固件（`FEATURE_BLE_DEFAULT=0`）：未单独构建验证  
- [x] **`FLIGHT ON` 后主机超时禁用**（代码路径）；断连不 ESTOP 逻辑已落地  

### 阶段 B — 控制与 ESC 对齐 400Hz

| 步骤 | 内容 |
|------|------|
| B1 | `CTRL_HZ=400`（或随 `esc_pwm_hz` 联动） |
| B2 | 台架：`FREQ 400` + 控制 400；空载 CLOSED，看超调/静差；**复核 PID/ki**（按 250 整定） |
| B3 | **保持采样 2k**；不降到 400/1200（仅为贴原文） |
| B4 | （可选）测速下沉：回调只写环缓；控制拍 6 点 LS |
| B5 | **明确 400Hz 调度方式**：简易 `vTaskDelayUntil`（接受 ~1ms 抖）或 **推荐 esp_timer→信号量** |

**验收**：

- [x] `FREQ 400` + `CTRL_HZ=400`：`CTRL?` meas_out_hz≈400；ESC 默认 400（短时闭环未达稳态转速，待长测复核超调）  
- [x] 急停 / ESTOP / HOST 策略按模式正确（含飞行态禁 HOST_TIMEOUT）  
- [x] 采样回归：`meas_hz≈2000`（本轮自检）；2000/4000/6000 霍尔交叉待长测  
- [x] 控制拍：esp_timer→信号量，`meas_out_hz≈400`  
- [x] USB 遥测默认 **20Hz**（每秒 20 帧完整 CSV；`TELEM?`）

### 阶段 C — 显式双核 pin（中等，建议 A/B 后）

| 步骤 | 内容 |
|------|------|
| C1 | `ctrlTask` pin Core1，高优；`loop` 降为通信泵 |
| C2 | 保持 esp_timer 采样；**不**把同步 SPI 放进硬 ISR（除非改 DMA）；采样回调**不绑定核心** |
| C3 | 测试态 BLE 留 Core0；飞行 `BLE OFF` 后观察采样抖动 / `enc_cpu_pct` |
| C4 | **`CORE?`**：打印控制核 / **采样回调实际运行核**（便于观察，勿假设 Core0） |

**验收**：

- [ ] `CORE?`：控制在 Core1、采样回调核可打印且稳定（不要求固定某一核）  
- [ ] 飞行关 BLE 后控制拍间隔抖动不恶化  

### 阶段 D — 测速算法增强（可选）

- 控制侧 6 点 LS vs 现窗差分+EMA：扑翼负载下对比超调、纹波、霍尔交叉误差。  
- 若上环缓：按 §4.4.1（深度 16/32、追尾丢旧、原子对）；`enc_ring.h`。  
- 符号锁定/EMA 在 flap 工况再整定。

### 阶段 E — 定点化（可选增强，非阻塞；见 §11.3）

| 步骤 | 内容 |
|------|------|
| E1 | 新增 `q16_math.h`（Q16.16 宏/乘除/限幅） |
| E2 | 先 EMA → 再 PID → 再 LS；`#define USE_FIXED_POINT` 可切浮点对照 |
| E3 | 遥测比对定点 vs 浮点，误差 **<0.5 RPM** 后再收敛移除浮点路径 |

### 8.1 主要改动文件（实施时）

| 文件 | 要点 |
|------|------|
| `ESP32ReadAS5047P.ino` | 模式开关、CTRL_HZ、可选 ctrlTask、环缓消费、命令、banner、`CORE?` |
| `ble_host.h` / `ble_host.cpp` | `bleEnd` / `bleSetEnabled`、关闭短路 |
| （可选）`enc_ring.h` | 深度 16/32、`(t_us, counts)`、读写 API、**追尾保护**（丢旧报警） |
| （可选）`q16_math.h` | Q16.16 定点库；PID/EMA/LS/前馈可逐步迁入 |
| 文档/脚本 | 测试不启 WiFi；飞行不依赖手机 NUS |

### 8.2 风险与缓解

| 风险 | 缓解 |
|------|------|
| 飞行关 BLE 后主机超时急停 | `FLIGHT ON` **自动禁用**或放宽 `HOST_TIMEOUT`；验收：断连不 ESTOP |
| `BLEDevice::deinit` 后再开不稳 | 飞行编译期关 BLE；测试用运行时 OFF |
| 控制 250→400 后 PID 变劲 | 复核超调/静差；必要时缩放 ki |
| ESC 400Hz 脉宽夹断 | 沿用现有 `applyEscPulse` 夹断（2000µs@2500µs 周期仍可行） |
| 拆任务后指令竞态 | mux 或「命令队列 + 控制拍消费」 |
| 测试态 BLE 与 SPI 同挤 Core0 | 测试可接受；飞行关 BLE；必要时降 BLE 遥测率 |
| `vTaskDelayUntil(2.5ms)` 周期抖 | 接受或改 esp_timer→信号量（§6.1） |
| 环缓写追读 | 丢旧+报警，禁止阻塞采样回调 |
| 回调内打印拖垮 2k | **禁止**回调 `Serial`/`printf` |

---

## 9. 测试计划衔接

### 9.1 已完成（2026-07-21，勿重复烧录争 COM）

| 项 | 结果 | 证据（`pc/logs/`） |
|----|------|-------------------|
| 转速计算正确 | PASS | `rpm_check_20260721_165919.*` |
| 霍尔定点减速比 | ≈27.744（≈设计） | `ff_pid_对比_20260721_182909.md` |
| 前馈重学 + PID/GainMap | 超调 ≤2.3% @2k–6k | `ff_eval_verify_20260721_182431.json` |
| 2kHz 采样 | 稳定 2000；交叉误差 <1% | `enc_highrate_20260721_190249.*` |
| 越过 7500 折叠界 | ~7300 仍吻合；动力饱和 | `enc_cross7500_20260721_190441.txt` |
| **7000RPM 阶梯首档** | 均值 ~6997，~21s，pulse~1496，未饱和 | `hi_step_7000_20260721_191733` |

收尾状态（文档记录）：电机已停（pulse=1000、rpm≈0）；COM 锁应空闲。**后续实物测试勿与其它任务抢 COM10。**

### 9.2 下一步实物（P0，用户再授权后）

**P0-1 高速阶梯（>7000，+500）**

- 闭环 + GainMap ON；从 >7000 起每 **+500RPM** 稳 **20s**。  
- **0.5s 保活重发 RPM**（防 HOST_TIMEOUT 1.5s ESTOP）；CSV 每秒 flush。  
- **到顶判据**（任一，避免空载共振「假平台」误判）：
  - 稳态均值不再升（增量 <~100RPM）**且** `pulse > 1950µs` → 动力上限；
  - `pulse` 仍在上升但 RPM 不再升（效率骤降）→ 亦判动力上限；
  - PID **u 贴上限** / pulse≈2000 / saturated；`settle_pulse≈ff_pulse` 且近 2000µs；
  - 编码器仍与霍尔吻合 → 动力/前馈问题，非测速。  
- **验收**：给出空载可达最高稳态 RPM + 到顶原因报告。

**P0-2 若要突破 ~7300**

- 路线 A：更高段重跑 LEARN 扩展前馈；路线 B：确认电池/电调动力硬上限。

**安全红线**

- 单 COM 锁（`com_port_guard.py`）；FAIL → FREQ 50 + 1000µs；≥2000RPM 运行；避免长时间低速顿挫；收尾 `_enc_safecheck.py`。

### 9.3 架构落地后的台架验收（衔接阶段 B）

在完成 CTRL/ESC 400 后，至少回归：

1. `_enc_idle_check.py`：banner / ENC? / 25 列  
2. 2000/4000/6000 闭环 + 霍尔交叉（可比对 `enc_highrate_*` 基线）  
3. 再续 P0-1 阶梯（若动力条件允许）

### 9.4 其它待办（非阻塞）

| 优先级 | 项 |
|--------|-----|
| P1 | 高采样率下 GainMap 覆盖 >5638RPM；参数固化写清默认值 |
| P2 | SPI-DMA / ABI+PCNT / MT6835；接扑翼后 flap 前馈重学 |

---

## 10. 关键路径索引

### 10.1 固件与烧录

| 项 | 路径 |
|----|------|
| 固件主文件 | `mcoder/ESP32_AS5047P/firmware/ESP32ReadAS5047P/ESP32ReadAS5047P.ino` |
| BLE | `…/ble_host.h`、`ble_host.cpp` |
| 烧录 | `mcoder/ESP32_AS5047P/flash_com10.bat`（备用 `flash_com18.bat`） |

### 10.2 本仓库文档（材料来源）

| 文档 | 内容 |
|------|------|
| **本文（根目录总纲）** | `E:\OEZCON\ESP32_AS5047P_转速采样与控制架构需求.md` |
| `关于转速采样的问题.txt` | 1200/400「高频采低频控」方案原文 |
| `20260722 项目计划评估.txt` | 外部评估 + 定点样例 + 浮点策略（已并入本文 §11） |
| `mcoder/ESP32_AS5047P/双核架构与采样改造建议_2026-07-22.md` | 双核、采集/使用≠输出、节拍对齐详解 |
| `mcoder/ESP32_AS5047P/高频采样改造_2026-07-21.md` | 2kHz 解耦改动与验证 |
| `mcoder/ESP32_AS5047P/工作计划与分析_2026-07-21_晚.md` | 全日交接、P0 阶梯、路径索引 |
| `mcoder/ESP32_AS5047P/工作日志_2026-07-21.md` | 时间线 |
| `mcoder/ESP32_AS5047P/扑翼霍尔接线_2026-07-21.md` | 霍尔接线 |
| `mcoder/ESP32_AS5047P/需求_电机转速控制_待确认_2026-07-18.md` | 早期需求边界 |

> 注：未找到独立文件 `架构改造实施需求_2026-07-22.md`；其意图内容已并入本文 §4–§8。

### 10.3 PC 脚本与日志（`mcoder/ESP32_AS5047P/pc/`）

| 脚本 | 用途 |
|------|------|
| `_enc_idle_check.py` | 停车自检 |
| `_enc_highrate_verify.py` | 2k/4k/6k + 霍尔 |
| `_enc_cross7500.py` | 越 7500 折叠界 |
| `_enc_safecheck.py` | 停车确认 |
| `_hi_step_7000.py` | 高速阶梯脚手架（扩展 +500） |
| `com_port_guard.py` | 单 COM 锁 |
| `encoder_monitor.py` | 监视 |
| `make_ff_pid_report.py` / `ff_pid_eval.py` | 前馈/PID 报告 |

日志目录：`mcoder/ESP32_AS5047P/pc/logs/`（见 §9.1）。

### 10.4 常用串口命令

| 命令 | 作用 |
|------|------|
| `ENC?` | 采样率 / CPU / Nyquist / counts / rpm 等 |
| `HALL?` | 霍尔与定点减速比 |
| `RPM <v>` / `RPMMAX <v>` | 目标转速 / 上限（≤12000） |
| `START` / `STOP` / `ESTOP` | 启停 / 急停 1000µs |
| `SOFT ON` / `SOFT RATE` | 斜坡 |
| `PID?` / `GAIN?` | PID / GainMap |
| `FREQ <hz>` | ESC PWM 频率（台架目标 **400**） |
| `BLE ON/OFF/?`、`FLIGHT ON/OFF/?`、`CTRL?`、`TELEM?`、`CORE?` | 阶段 A/B：通信模式 / 输出频率 / USB 遥测帧率 / 核诊断 |

### 10.5 遥测格式要点

- **USB 遥测帧率（写死）**：
  - **Hz = 完整遥测帧率**＝每秒 **N 行** CSV（**1 帧 = 1 行完整 CSV 全字段**），**不是**帧内计算次数。
  - 原先草案「**100Hz**」＝**100 帧/秒**；现用户定默认 **`usb_telem_hz=20`** → **20 帧/秒**。
  - 与 **采集2k / 使用2k / 输出400 无关**；遥测只读快照并打印。查询：`TELEM?`。
- USB CSV：旧列不变，**第 25 列 `enc_sample_hz`**。  
- BLE lite：`B,t_ms,motor_rpm,pulse,target,mode,run,out_hz_meas,gear_meas`（默认亦 ~20Hz）。  
- 霍尔换算电机 RPM = `out_hz × 27.744 × 60`。
- **硬约束**：2k 采集回调 / 400 输出任务内禁止串口打印；meta 入异步环由 `loop` 排出。

### 10.6 接手自检（不抢 COM 时）

```powershell
# 仅在确认无其它任务占用 COM10 时执行
$env:PYTHONIOENCODING="utf-8"
python _enc_idle_check.py
```

固件重烧：`flash_com10.bat`（需单独授权；勿与并行测试任务冲突）。

---

## 11. 外部评估与反馈（2026-07-22）

> **来源**：`E:\OEZCON\20260722 项目计划评估.txt`（三部分：评估 / 样例程序 / 浮点处理）。  
> **对照**：本文 §1–§10、`双核架构与采样改造建议_2026-07-22.md`、固件现状（使用≈2kHz、输出 250、Arduino-ESP32+FreeRTOS）。  
> **结论**：总纲成熟可交接；**可直接进入阶段 A/B**。下列为已采纳确认、须修正处与实施清单增补。

### 11.1 评估结论摘要

| 维度 | 评价要点 |
|------|----------|
| 总体 | 达到「可交接总纲」水准；逻辑/术语/数据流写死；「高频采集/使用，低频输出」正确（旧「采/取用」说法已废） |
| **应坚持** | 采集保持 2k；使用≠输出；弃 250、采 400/200/2000 整数系；Arduino-ESP32+FreeRTOS；飞行关 BLE |
| **最需补两点** | ① 400Hz 控制是否用硬件定时器（`vTaskDelayUntil` 精度不足须注明或改方案）；② 若上环缓须提前定义深度/API/追尾保护 |
| 可实施性 | 阶段清晰；环缓细节曾留白 → 已写入 §4.4.1 |
| 风险 | 原缺「环缓写追读」→ 已写入 §8.2 |

**外部确认正确、本计划已采纳（无需改方向）**：

- 不为贴原文降采样到 1200/400  
- 术语隔离：400 = 输出 ≠ 使用（「取用」作废）  
- 弃 250、对齐 ESC 400  
- 不迁纯 IDF  
- 测试/飞行双态通信  

### 11.2 样例程序要点（文内代码，无独立仓库文件）

评估文**未指向**仓库内独立样例路径；关键接口与任务结构均以文内 C/C++ 摘录给出。核对固件：尚无 `enc_ring` / `q16_math` / `USE_FIXED_POINT`。

#### 11.2.1 Q16.16 库接口（建议落地为 `q16_math.h`）

```cpp
#define Q16_16_FRAC_BITS  16
#define Q16_16_SCALE      (1 << 16)
#define FLOAT_TO_Q16(x)   ((int32_t)((x) * Q16_16_SCALE + 0.5f))
#define Q16_TO_FLOAT(x)   ((float)(x) / Q16_16_SCALE)
#define INT_TO_Q16(x)     ((int32_t)(x) << 16)
#define Q16_ADD(a, b) / Q16_SUB(a, b)
int32_t q16_mul(int32_t a, int32_t b);   // int64 乘后 >>16
int32_t q16_div(int32_t a, int32_t b);   // 除零 → INT32_MAX
int32_t q16_clamp(int32_t x, int32_t lo, int32_t hi);
```

#### 11.2.2 算法定点入口（控制侧）

| 模块 | 接口形态 | 要点 |
|------|----------|------|
| PID | `pid_update_q16(pid_q16_t*, target, feedback, dt_q16)` | `dt` 须 Q16（如 `FLOAT_TO_Q16(2.5f)`）；积分限幅 |
| EMA | `ema_update_q16(current, raw, alpha_q16)` | α 与 1−α 均为 Q16 |
| 6 点 LS | `calc_rpm_ls_q16(enc_sample_q16_t*, n)` | 斜率→RPM；`t` 为 µs 时 ×60e6/CPR |
| 前馈 | `ff_lookup_q16(target_rpm, table, len)` | 表项 `(rpm, pulse)` 均为 Q16，线性插值 |

#### 11.2.3 环缓 + 采样回调结构（阶段 D 可选）

```cpp
typedef struct {
    int32_t t_us;     // 可用原始 µs 或 Q16；样例示 Q16
    int32_t counts;   // 解缠累加
} enc_sample_q16_t;

// 采样回调（IRAM_ATTR）：SPI → unwrap → enc_ring_push →（可选）勿做打印
void IRAM_ATTR encSampleCallback(void *arg);

// 控制任务：等信号量（推荐 esp_timer@400Hz 释放）→ peek 近 N 点 → LS/PID → ESC
```

**教了什么**：线性控制链路（差分/PID/EMA/LS/前馈）适合定点；环缓存 `(t, counts)`、控制侧算速；**只在遥测打印转浮点**。

### 11.3 浮点数处理策略（定点优先）

| 决策 | 内容 |
|------|------|
| **统一格式** | **Q16.16**（`int32_t`）；RPM/脉宽/PID 系数/dt 统一，避免混用 Q8.24 等 |
| **为何** | 链路全是线性运算；定点更快、时序更确定；精度满足 ±0.1 RPM |
| **边界** | 乘用 `int64` 防溢；除查零；变量一律 `int32_t` |
| **调试** | 保留 `Q16_TO_FLOAT`；遥测仍可读 |
| **迁移** | `q16_math.h` → EMA → PID → LS；`USE_FIXED_POINT` 对照；误差 <0.5 RPM 再删浮点路径 |
| **与环缓** | 样例可将环内点存 Q16；亦可环内存原始整数、仅在控制算速时进 Q16——实施时二选一写死 |

预估（外部）：400Hz 控制拍定点后总耗时约 **3–5µs**（浮点约 15µs 量级），CPU 占比极低——**非阻塞增强**，不挡阶段 A/B。

### 11.4 对照计划：已采纳 / 须修正 / 写入实施清单

| 类别 | 项 | 落点 |
|------|----|------|
| **已采纳（坚持）** | 2k 采集、使用≠输出、弃 250、Arduino+FreeRTOS、飞行关 BLE | §1 / §2 / §5 / §6 / §7 |
| **须修正（已改本文）** | 勿写死 esp_timer「常见 Core0」 | §3.3 / §4.2 |
| | `vTaskDelayUntil(2.5ms)` 非精确 2.5ms；推荐 esp_timer→信号量 | §4.2 / §6.1 / 阶段 B5 |
| | 回调瘦身非时序刚需（<20µs）；禁回调打印 | §4.4 |
| | 环缓深度/追尾/原子性 | §4.4.1 / §8.1 / §8.2 |
| | `FLIGHT ON` 禁用主机超时；验收断连不 ESTOP | §5 / 阶段 A |
| | 到顶判据结合 `pulse>1950µs` | §9.2 |
| | `CORE?` 打印回调实际核 | 阶段 C4 |
| **写入实施清单** | B5：选定 400Hz 调度方案 | §8 阶段 B |
| | D：环缓 API + 追尾 | §8 阶段 D |
| | E：`q16_math.h` 定点迁移 | §8 阶段 E |
| | A 验收：飞行态断连不 ESTOP | §8 阶段 A |



> **实施状态（2026-07-22）**：主线 A/B 已 git 存档。阶段 D/E（`enc_ring` / `q16_math`）以备份开关落地：`USE_FIXED_POINT` 默认 0，浮点 2k 主路径不变；启用见 `mcoder/ESP32_AS5047P/firmware/ESP32ReadAS5047P/README_Q16_BACKUP.md`。待硬件验证。

### 11.5 外部建议的可选增强（非阻塞）

| 增强项 | 收益 | 成本 |
|--------|------|------|
| 控制任务 esp_timer→信号量 | 400Hz 精度 ±1ms → ±~20µs | 低（约数十行） |
| 环缓取代 portMUX 快照 | 控制侧可做 6 点 LS | 中（约百行+API） |
| 采样回调禁 Serial.print | 防回调超时 | 代码审查 |
| `FLIGHT ON` 自动禁主机超时 | 防空中急停 | 低（约十行） |
| Q16.16 全链路 | 控制拍更快更确定 | 中（库+逐步替换） |

---

## 附录 A — 参数对照速查

| 参数 | 方案建议 | 本工程现状 | 目标 |
|------|----------|------------|------|
| 采集（SPI） | 1200Hz 首选 | **2000Hz** | **保持 2000**（≥1200） |
| 使用（求速） | 控制侧 6 点 LS @400；或相邻差分≈采集率 | 回调内 8 点窗+EMA **@≈2000**（采 2k 用 2k） | 保持同层 2k；可选迁 LS |
| 输出（PID→ESC） | 400Hz | **250Hz** | **400Hz**（调度：esp_timer 信号量优先，或接受抖的 `vTaskDelayUntil`） |
| ESC | 400Hz | 默认 50，可 FREQ | **FREQ 400** |
| 系统节拍 | — | 嵌套 250 | 可选 **200Hz** |
| 定时器 | esp_timer | esp_timer ✅ | 采集保持；输出推荐亦用 esp_timer 触发 |
| 同步 | 环缓+semaphore | portMUX 快照 | 可演进环缓（深度 16/32，追尾丢旧） |
| 数值 | — | 浮点为主 | **可选 Q16.16 定点**（§11.3） |
| 框架 | — | Arduino-ESP32 | **保持** |
| BLE | — | 上电即开 | 测试开 / 飞行关 |
| WiFi | — | 未用 | 保持关 |
| 主机超时 | — | 1.5s | 测试保留；**飞行禁用/放宽** |

## 附录 B — 一句话交接

**已做到**：2kHz 硬定时采集/使用（采 2k 用 2k）+ 250Hz 输出 + 测速不折叠 + 7000 阶梯首档通过。  
**下一步架构**：弃 250；输出与 ESC 对齐 **400**（注意 tick 抖或改 esp_timer 触发）；可选 **200** 系统节拍；多任务分频 + Core1 控制；测试 BLE / 飞行关 BLE（并禁主机超时）；**使用保持 2k，绝不把 400 叫成使用**（「取用」作废）；可选环缓/Q16。  
**下一步实物**：用户授权后从 7000 起 **+500** 阶梯探动力上限（到顶须看 pulse）；单占 COM10。

---

*合并修订：2026-07-22 · 根目录唯一总纲 · 术语统一（采集 / 使用 ≠ 输出；「取用」作废）· 节拍整数系（弃 250）· 通信双态 · Arduino-ESP32 选型写死。*  
*评估并入：2026-07-22 · 增 §11 外部评估与反馈 · 修正 400Hz 调度/环缓/飞行超时/到顶判据/esp_timer 核表述 · 定点策略入实施清单。*  
*术语修订：2026-07-22 · 废弃「取用」；正式用语：采集=2kHz SPI、使用=2kHz 求速（采2k用2k）、输出=400Hz PID→ESC。*

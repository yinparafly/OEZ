# 气球投放飞行方案 — PX4 修改分析

## 一、飞行剖面

```
Phase 1          Phase 2         Phase 3           Phase 4        Phase 5
[释放]    →    [自由下落]   →   [拉起过渡]    →   [电机启动]  →  [巡航导航]
气球上          头部垂直朝下       Z轴1.7g过载        推力接替         飞向航点
                无滚转            俯仰→水平          油门递增
                空速递增           至水平状态          进入航线
```

**详细时序：**
1. 从气球释放，飞机头部垂直朝下（俯仰 -90°）
2. 自由下落阶段：无滚转控制（保持航向），无推力，靠重力加速
3. 当空速达到大巡航空速的 70% 时，触发拉起动作
4. 拉起：Z 轴过载 1.7g，从 -90° 俯仰拉到 0°（水平），约 90° 俯仰变化
5. 达到水平状态后，启动电机，油门从零递增
6. 进入自动驾驶巡航模式，飞向第一个航点

---

## 二、关键参数计算

### 2.1 拉起阶段的几何和力学分析

假设条件（以典型固定翼为例）：
- 大巡航速度 V_cruise ≈ 25 m/s
- 目标触发起始空速 V_trigger = 0.7 × V_cruise = 17.5 m/s
- 拉起过载 n = 1.7g

**拉起半径：**
```
R = V² / (g × (n - 1))
R = 17.5² / (9.81 × 0.7)
R = 306.25 / 6.867
R ≈ 44.6 m
```

**拉起过程中的高度损失：**
从 -90° 拉到 0°，飞机轨迹是圆弧的 1/4：
```
Δh = R × (1 - cos(90°)) = R × 1 = 44.6 m（从弧顶到弧底）
但实际是从竖直向下开始，高度损失约为：
Δh ≈ R = 44.6 m
```

**拉起时间：**
```
弧长 = R × π/2 = 44.6 × 1.571 ≈ 70.1 m
时间 t = 弧长 / V_avg ≈ 70.1 / 17.5 ≈ 4.0 s
```

**结论：** 需要至少 **45-50 米**的净空高度来完成拉起动作。从气球释放时应确保距地面至少 80-100 米。

### 2.2 释放高度建议

- 最低释放高度：100 米（含 50m 拉起 + 30m 余量 + 20m 电机启动过渡）
- 推荐释放高度：150-200 米
- 自由下落时间（从释放到触发）：
  ```
  V = g × t → t = 17.5 / 9.81 ≈ 1.8 s（忽略空气阻力）
  实际考虑阻力后约 2-3 秒
  ```

---

## 三、PX4 代码修改方案

### 方案 A：新建自定义飞行模式（推荐）

在 `src/modules/flight_mode_manager/tasks/` 下新建 `BalloonDrop` 任务。

**文件结构：**
```
src/modules/flight_mode_manager/tasks/BalloonDrop/
├── FlightTaskBalloonDrop.hpp
├── FlightTaskBalloonDrop.cpp
```

#### FlightTaskBalloonDrop.hpp

```cpp
#pragma once

#include "FlightTask.hpp"
#include <lib/ecl/geometry/utility.h>

class FlightTaskBalloonDrop : public FlightTask
{
public:
    FlightTaskBalloonDrop(module_params *parent);
    ~FlightTaskBalloonDrop() = default;

    bool activate(const ModeFlags &mode_flags) override;
    void updateSetpoint() override;

private:
    enum class DropPhase : uint8_t {
        FREE_FALL,      // Phase 1-2: 自由下落，保持航向
        PULL_UP,        // Phase 3: 拉起过渡
        MOTOR_START,    // Phase 4: 电机启动
        CRUISE          // Phase 5: 巡航导航
    };

    DropPhase _phase{DropPhase::FREE_FALL};

    // 参数
    float _trigger_airspeed_ratio{0.7f};  // 触发拉起的空速比例
    float _pullup_load_factor{1.7f};       // 拉起过载 g 值
    float _cruise_airspeed{25.0f};         // 大巡航速度 m/s
    float _motor_start_alt{0.0f};          // 电机启动延迟（秒）

    // 内部状态
    float _pullup_start_altitude{0.0f};
    float _pullup_target_pitch{0.0f};      // 目标俯仰角
    float _motor_ramp_time{2.0f};          // 油门爬升时间（秒）

    void _updateFreeFall();
    void _updatePullUp();
    void _updateMotorStart();
    void _updateCruise();
};
```

#### FlightTaskBalloonDrop.cpp 核心逻辑

```cpp
#include "FlightTaskBalloonDrop.hpp"
#include <uORB/topics/vehicle_air_data.h>
#include <uORB/topics/airspeed.h>

FlightTaskBalloonDrop::FlightTaskBalloonDrop(module_params *parent)
    : FlightTask(parent)
{
}

bool FlightTaskBalloonDrop::activate(const ModeFlags &mode_flags)
{
    _phase = DropPhase::FREE_FALL;
    _pullup_start_altitude = _position[2];  // 当前高度
    return true;
}

void FlightTaskBalloonDrop::updateSetpoint()
{
    switch (_phase) {
    case DropPhase::FREE_FALL:
        _updateFreeFall();
        break;
    case DropPhase::PULL_UP:
        _updatePullUp();
        break;
    case DropPhase::MOTOR_START:
        _updateMotorStart();
        break;
    case DropPhase::CRUISE:
        _updateCruise();
        break;
    }
}

void FlightTaskBalloonDrop::_updateFreeFall()
{
    // Phase 1-2: 保持航向（无滚转），俯仰 -90°（头部朝下）
    // 不输出推力（油门 = 0）
    // 不输出姿态控制力矩（靠气动稳定性）
    // 仅保持偏航锁定

    _position_setpoint[0] = _position[0];  // 保持当前位置
    _position_setpoint[1] = _position[1];
    _position_setpoint[2] = _position[2];  // 不控制高度

    // 航向保持（锁定释放时的偏航角）
    _yaw_setpoint = _yaw;

    // 油门 = 0
    _thrust_setpoint = 0.0f;

    // 检查触发条件：空速 >= 0.7 × 巡航空速
    float trigger_airspeed = _trigger_airspeed_ratio * _cruise_airspeed;
    if (_airspeed >= trigger_airspeed) {
        _phase = DropPhase::PULL_UP;
        _pullup_start_altitude = _position[2];
    }
}

void FlightTaskBalloonDrop::_updatePullUp()
{
    // Phase 3: Z 轴过载 1.7g 拉起
    // 从俯仰 -90° 拉到 0°
    // 使用角速度控制实现恒定过载拉起

    float n = _pullup_load_factor;  // 1.7
    float V = _airspeed;
    float omega = (n - 1.0f) * 9.81f / V;  // 俯仰角速度 rad/s

    // 设置俯仰角速度指令
    _attitude_setpoint = {};  // 清空
    _attitude_setpoint.pitch_rate = omega;  // 正值 = 抬头

    // 保持偏航锁定
    _attitude_setpoint.yaw_rate = 0.0f;

    // 油门仍然 = 0（纯滑翔拉起）
    _thrust_setpoint = 0.0f;

    // 检查是否达到水平状态（俯仰角 > -10°）
    float pitch = _euler_pitch();  // 当前俯仰角
    if (pitch > math::radians(-10.0f)) {
        _phase = DropPhase::MOTOR_START;
    }
}

void FlightTaskBalloonDrop::_updateMotorStart()
{
    // Phase 4: 启动电机，油门线性递增
    // 姿态控制切换到水平巡航姿态

    // 油门递增（2 秒内从 0 到巡航油门）
    static float motor_start_time = 0.0f;
    motor_start_time += _dt;

    float cruise_throttle = 0.6f;  // 巡航油门比例（需根据实际飞机调整）
    float ramp_progress = motor_start_time / _motor_ramp_time;
    if (ramp_progress > 1.0f) ramp_progress = 1.0f;

    _thrust_setpoint = cruise_throttle * ramp_progress;

    // 姿态保持水平
    _attitude_setpoint.roll = 0.0f;
    _attitude_setpoint.pitch = 0.0f;
    _attitude_setpoint.yaw_rate = 0.0f;

    // 油门达到巡航值后进入巡航模式
    if (ramp_progress >= 1.0f) {
        _phase = DropPhase::CRUISE;
    }
}

void FlightTaskBalloonDrop::_updateCruise()
{
    // Phase 5: 完全自动驾驶巡航
    // 切换到 Position 模式，飞向第一个航点
    // 这里实际应该由 navigator 模块接管

    // 在实际实现中，这里应该发出一个标志
    // 让 commander 切换到 Position/Auto 模式
    _mission_item_triplet_valid = true;
}
```

### 方案 B：修改现有模块（更简单但侵入性更大）

#### 修改 commander 模块

在 `src/modules/commander/ModeUtil/mode_util.cpp` 中添加气球投放模式检测：

```cpp
// 在 mode 字段中添加 BALLOON_DROP 模式
// 在 flight_mode_manager 中注册新的 FlightTask
```

#### 修改 flight_mode_manager

在 `src/modules/flight_mode_manager/FlightModeManager.cpp` 中注册：

```cpp
#include "tasks/BalloonDrop/FlightTaskBalloonDrop.hpp"

// 在任务注册表中添加
FlightTask *FlightModeManager::_createFlightTask(FlightTaskIndex_t index)
{
    switch (index) {
    // ... existing tasks ...
    case FlightTaskIndex::BalloonDrop:
        return new FlightTaskBalloonDrop(this);
    // ...
    }
}
```

#### 修改任务索引枚举

在 `src/modules/flight_mode_manager/FlightModeManager.hpp` 中添加：

```cpp
enum class FlightTaskIndex : uint8_t {
    // ... existing indices ...
    BalloonDrop = XX,  // 选择一个空闲编号
};
```

### 方案 C：使用 MAVLink 命令触发（最简单，无需改代码）

利用现有 Offboard 模式 + MAVLink 命令序列：

1. 地面站发送 MAV_CMD_DO_SET_MODE 切换到 Offboard 模式
2. 通过 MAV_CMD_NAV_WAYPOINT 发送姿态指令序列
3. 用 Python 脚本（MAVSDK/DroneKit）编排整个流程：

```python
# 伪代码
await drone.set_mode("OFFBOARD")

# Phase 1-2: 保持航向，等待空速
while drone.airspeed < cruise_speed * 0.7:
    await drone.set_attitude(pitch=-90, roll=0, throttle=0)
    await asyncio.sleep(0.1)

# Phase 3: 拉起
while drone.attitude.pitch < -10 degrees:
    # 恒定俯仰角速度 = (n-1)*g / V
    omega = (1.7 - 1) * 9.81 / drone.airspeed
    await drone.set_attitude_rate(pitch_rate=omega, throttle=0)
    await asyncio.sleep(0.1)

# Phase 4: 启动电机
for t in range(20):  # 2 秒渐增
    throttle = 0.6 * t / 20
    await drone.set_attitude(pitch=0, roll=0, throttle=throttle)
    await asyncio.sleep(0.1)

# Phase 5: 切换到 Auto 模式
await drone.set_mode("AUTO")
```

**优点：** 不需要修改 PX4 源码，快速验证
**缺点：** 依赖通信链路，延迟可能导致拉起失败

---

## 四、推荐方案

**短期验证：** 方案 C（MAVLink 脚本），快速测试飞行剖面
**长期部署：** 方案 A（自定义 FlightTask），完全自主不依赖通信

### 修改文件清单（方案 A）

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/modules/flight_mode_manager/tasks/BalloonDrop/FlightTaskBalloonDrop.hpp` | 新建 | 头文件 |
| `src/modules/flight_mode_manager/tasks/BalloonDrop/FlightTaskBalloonDrop.cpp` | 新建 | 核心逻辑 |
| `src/modules/flight_mode_manager/FlightModeManager.hpp` | 修改 | 添加枚举索引 |
| `src/modules/flight_mode_manager/FlightModeManager.cpp` | 修改 | 注册新任务 |
| `src/modules/flight_mode_manager/CMakeLists.txt` | 修改 | 添加新源文件 |
| `boards/radiolink/PIX6/default.px4board` | 可选修改 | 启用/禁用相关模块 |

### 需要调整的参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| TRIGGER_AIRSPEED_RATIO | 0.7 | 触发拉起的空速比例 |
| PULLUP_LOAD_FACTOR | 1.7 | 拉起过载 g 值 |
| CRUISE_AIRSPEED | 25.0 | 大巡航速度 m/s |
| MOTOR_RAMP_TIME | 2.0 | 电机启动时间 s |
| CRUISE_THROTTLE | 0.6 | 巡航油门比例 |

---

## 五、安全注意事项

1. **最低释放高度：** 80-100 米（考虑 45m 拉起 + 余量）
2. **空速传感器校准：** 自由下落阶段空速测量可能受扰流影响，建议使用皮托管
3. **GPS 锁定：** 释放前确保 GPS 已锁定（至少 10 颗星）
4. **地面站监控：** 首次测试建议在视距内、开阔场地进行
5. **回收伞：** 建议加装备份降落伞，万一拉起失败可安全着陆
6. **电机保护：** 拉起阶段电机不工作，确保螺旋桨不干涉气流

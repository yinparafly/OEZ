# 工作日志 · 2026-07-19（约 08:30–17:30）

台架：ESP32-S3 + AS5047P + 航模 ESC，空载（noload），串口 COM10；午后增加 BLE 遥控链路。

---

## 上午 · 控制栈与空载闭环

| 时段（约） | 事项 | 结果 |
|------------|------|------|
| 09:00–10:20 | 开环学习（台阶/斜坡）、前馈图 + GainMap；分析死区（约 1100 μs 起转） | S0 完成；闭环宜从 ≥1000 RPM 起 |
| 10:30–11:05 | S1 升/降不对称斜坡 `SOFT RATE UP/DOWN`；烧录、自动 A1/A2 | PASS（DOWN 放慢则降速入带变长） |
| 11:05–11:10 | S2 加速度前馈 Ka；对比 Ka=0 vs 0.05 | PASS；保留 Ka≈0.05 |
| 11:10–11:15 | 空载多点闭环 1000…6000 | 6/6 PASS |

## 中午前后 · 方案修订与扫频

| 时段（约） | 事项 | 结果 |
|------------|------|------|
| 11:15–11:25 | 按实测修订《分歧实测验证方案》；补 T1′ 规则、COM 锁、PING | 方案落地 |
| 11:20–11:35 | 发现 OPEN+PWM 被控制环盖回怠速；改 MEASURE 开环锁存 + `open_pwm_hold`；套件 S1/S2/多点回归 | 套件 PASS |
| 11:35–11:50 | PWM `FREQ` 上限扩至 **600 Hz**（高刷新脉宽夹断）；烧录；T1′ 重扫 50→600 | **全 PASS**，冻结工作默认 **600 Hz** |

## 午后 · 协议与控制收口

| 时段（约） | 事项 | 结果 |
|------------|------|------|
| 12:00–12:10 | T2 DShot150/300/600（固件 RMT + `auto_dshot_test`）；thr=248/500 两次 | **FAIL@150** → 锁定 **PWM** |
| 12:05–12:10 | T4 停转→1000 RPM ×3 | PASS，维持停转策略 |
| 12:10–12:20 | S3 ADG（减速 Ki/积分限 + 加减速双增益）；BASE/SCALE/DUAL 对比 | 三组 PASS；**空载默认 ADG OFF** |

## 午后 · FREQ=400 分步闭环

| 时段（约） | 事项 | 结果 |
|------------|------|------|
| 12:35–12:45 | V1→V6→F（`auto_freq400_staged`）：开环→map 对比→1000→中高点→A1/A2→全阶梯 | **全 PASS**；工作默认改为 **FREQ=400** |
| 12:50–12:57 | 50 vs 400 同会话闭环 A/B（C1 阶梯+C2 动态+C3 曲线） | **相当/噪声内**；跟踪未系统性更好 |

## 傍晚 · BLE 遥控 + 手机简控

| 时段（约） | 事项 | 结果 |
|------------|------|------|
| 15:00–16:00 | 固件加 Nordic UART（`ble_host`），设备名 **OEZ-RPM**；USB 全量遥测 + BLE 瘦身 `B,...`（默认 20 Hz） | 烧录可用；Windows Notify 不稳，READ 兜底可用 |
| 16:00–16:30 | PC：`ble_link.py` + `encoder_monitor` 链路选 BLE；写说明 `蓝牙版使用说明_2026-07-19.md` | `python encoder_monitor.py --ble` 可连 |
| 16:30–17:30 | Android **MotorBleControl**（非听声 MotorPitchRpm）：扫描/连接、大字转速、启停、设定/预设、近 8 s 曲线 | v1.0.3；Notify 不稳时用 GATT READ 轮询；无数据显示「未收到」 |

### 手机 APK（调试包）

```text
android/MotorBleControl/app/build/outputs/apk/debug/app-debug.apk
versionName 1.0.3
```

协议：`RPM` / `START` / `STOP` / `PING`；入站 `B,t_ms,rpm,pulse,target,mode,run`（与固件一致，App 侧未强制改固件协议）。

---

## 当日主要产出（代码/文档）

- 固件：`SOFT UP/DOWN`、`KA`、`ADG*`、`PROTO/DSHOT*`、`FREQ 50..600`、`open_pwm_hold`、MEASURE 开环、**BLE NUS（ble_host）**
- 脚本：`auto_freq_test` / `auto_dshot_test` / `auto_s1` / `auto_s2` / `auto_s3_adg` / `auto_t4` / `auto_full_suite` / `com_port_guard`
- PC：`ble_link.py`、encoder_monitor BLE 链路、`requirements.txt` + bleak
- Android：`android/MotorBleControl/`（电机+编码器 BLE 简控）
- 文档：分歧方案修订、控制方法备忘、**蓝牙版使用说明**、本日工作日志/待办

## 保留的结论性报告（过程件已删）

| 文件 | 用途 |
|------|------|
| `工作日志_2026-07-19.md` / `工作总结与待办_2026-07-19.md` | 本日汇总 |
| `蓝牙版使用说明_2026-07-19.md` | BLE 烧录/PC/降带宽 |
| `PWM频率冻结表_20260719_114733.md` | T1′ 开环至 600；闭环工作默认改 400 |
| `FREQ400分步记录_20260719_124226.md` | V1–V6 + F 全 PASS |
| `DShot冻结表_20260719_120633.md` | T2 不通 → PWM |
| `T4停转启动报告_20260719_120729.txt` | 停转策略 |
| `S3_ADG对比报告_20260719_121439.txt` | S3 对比与默认 OFF |
| `空载多点测试报告_20260719_112953.md` | 1000…6000 |
| `空载全自动测试报告_20260719_112811.md` | 套件回归 PASS |
| `pc/logs/`：上表对应最终数据 + S1/S2 首次验收 + 学习 `094711` | 原始数据 |

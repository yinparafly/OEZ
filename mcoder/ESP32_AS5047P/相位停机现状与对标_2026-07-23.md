# 相位停机现状与对标 · 2026-07-23

> 对照 [演进计划](相位停机控制演进计划_2026-07-23.md) P0–P6 + 外部同类方法检索 + 本轮 COM10 实验。  
> **停点**：P0 ✅（2026-07-23）；证据 pc/logs/hold_phase_bias_learn_20260723_105128.txt。  
> 硬约束不变：**单向 ESC / 无主动刹 / 霍尔 0·180 / 互质齿每沿重锁**。

---

## 1. 相对计划的进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| **基线** | ✅ | APPROACH 距离规划 `k·rpm²` + `desired_rpm`；BRAKE 禁误差加油；REFINE Hall；bias 脚本 |
| **P0 修入口** | ✅ **已完成** | 爬行带门闬 + 禁 CRAWL_PULSE_MIN 托底；rake_k 可学；4-iter err≈1.8–9.6°（…105128.txt）。残留 stall90 抖 → P1 |
| **P1 规则监督** | 🟡 部分 | R-crawl120 已落地；`REDFLAG` 日志（>90/120/200 + A/B/C）本轮补；完整 health/too-fast/打分仍待 |
| **P2 bias/OFFSET↔k** | 🟡 部分 | 台架 bias 脚本可用；**单次 FRAME 不再自动 OFFSET**（防误翻半圈）；`brake_k` EMA 仍难涨 |
| **P3–P6** | ⏸ | 未开；等 P0 入口稳定 + `learn_n` 可增 |

**当前阻塞（按优先级）**

1. ~~P0 软降出口须入爬行带~~ → **已修**（松油替代 `CRAWL_PULSE_MIN` 托底）。  
2. ~~`brake_k` 学不动~~ → **learn_n 已增**；仍建议过滤 coast≪2° 样本。  
3. **SOFTDONE 后 stall90 抖**：松油过猛 → RPM≈0 → 再踢启；需 kick 剖面。  
4. **`REDFLAG` 刷屏**：stall 路径每拍打印，需统一节流。  
5. soft remain 常 >90：相位随机，标 B 观察即可，勿当 A 翻 OFFSET。

---

## 2. 外部方法对标（检索摘要）

| # | 方法 / 领域 | 要点 | 与本机拟合 |
|---|-------------|------|------------|
| 1 | **欠驱动 + 单向执行器停车**（朋友建议 / RC ESC race profile） | 目标窗内唯一动作=松油；禁止误差→加油 | ✅ **已采纳**；继续坚持 |
| 2 | **Coast-down / 滑行距离图**（汽车 coast-down、`d∝v²` 气动项） | 用 `(v, coast)` 辨识阻力；表或 `k·v²` | ✅ 已是 Scheme B；**P3** 可加分速带表；样本须排除红旗/标架 |
| 3 | **制动距离查表 / 输送线停位** | 工业输送常有主动刹；无刹时靠「提前量 + 惯量」 | ⚠️ 主动刹不可用；**提前量+学习 coast** 可借鉴，勿抄液压软刹 |
| 4 | **监督混合自动机**（mode / guard / reset；P1/P5） | 离散策略切换：soft → plan → cut → refine | ✅ 与计划监督层一致；先做 **规则 R-***，再 scores+迟滞 |
| 5 | **Throw-and-catch / 接触规划**（MIT underactuated） | 无驱段靠预测落点；有驱段只对准释放窗 | ✅ 概念同构：APPROACH=对准释放窗，BRAKE=无驱滑行 |
| 6 | **霍尔+磁编相位归零**（扑翼/摆线机构） | 稀疏绝对标记 + 连续积分；互质齿须重锁 | ✅ 已做每沿 latch；~200° 优先查 OFFSET |

**值得马上试的外来招（仍先易后难）**

- **故意中速切油采 coast**（诊断用）：在 remain≫est 时短时 ESTOP/1000µs，只为喂 `brake_k`，与正式 HOLD 分离。  
- **Capture 窗**（计划 P4）：窗外只减速，窗内才允许 cut——减少 `remain_was=0` 才切油。  
- **软降出口用测速 guard，不用纯时间**（本轮已改）。

---

## 3. 计划微调（保持先易后难）

不改 try-order，只收紧 P0 验收与并行小项：

| 调整 | 内容 |
|------|------|
| **P0 加严** | `SOFTDONE` 必须 `rpm ∈ [0.4·crawl, crawl+150]`；壁钟到仍高速 → `soft_overrun` REDFLAG + crawl，**不**宣告 SOFTDONE |
| **P0∥诊断** | 单独「旋转中切油」脚本喂 `learn_n`；正式 HOLD 仍以停相为准 |
| **P1 提前落日志** | `REDFLAG lvl/branch/where` 可先于完整监督器（本轮已有） |
| **P2 纪律** | 单次 `|bias|≳150` **不**自动 `OFFSET±180`；需连续 2 次 FRAME |
| **仍禁止** | 跳到 P6 / 反向刹幻想；用调 k 掩盖半圈标架 |

---

## 4. 本轮实验（COM10）

| 项 | 内容 |
|----|------|
| 固件 | `SOFTDONE` 爬行带门闩；高速段强制 `1000µs` 松油（禁 `CRAWL_PULSE_MIN`≈3500RPM 托底）；`REDFLAG`；coast 按 `esc_sense`；REFINE 过速不切 |
| 脚本 | 红旗分 A/B/C；单次 FRAME 不翻 OFFSET |
| 烧录 | `flash_com10.bat`（二次：coast 修） |
| 复测日志 | `pc/logs/hold_phase_bias_learn_20260723_105128.txt` · `*_run_p0b.out` |

### 4.1 关键结果（4 iter，`stop_ms=4000`）

| 指标 | 结果 |
|------|------|
| SOFTDONE rpm | **~117–291**（不再 ~3500） |
| `brake_k` / `learn_n` | **0.0008→~0.00057，learn_n=3**（首次 EMA 成功） |
| 停相误差 | ~1.8° / 6.4° / 7.9° / 9.6°（均 <90°；1× PASS 带内，3× STRATEGY 靠 REFINE） |
| OFFSET | **未误翻**（保持 0） |
| 残留问题 | SOFTDONE 后仍有 **approach_stall90** 短时 RPM 塌再踢启；`REDFLAG` 刷屏需节流；soft remain 仍常 >90（相位随机，属可接受红旗观察） |

### 4.2 对照旧跑（`…102736.txt`）

- 旧：SOFTDONE rpm≈3480，learn_n=0，3/5 PASS，1× 误 OFFSET=180  
- 新：入口入带 + k 可学；P0 主路径显著改善，未完全关掉 stall 抖

---

## 5. 下一步（建议顺序）

1. **P0 收尾**：SOFTDONE 后 kick 用更高 CLOSED 爬行，减少 stall90 抖；`REDFLAG` 800ms 节流（已有 soft_overrun，扩到 stall）。  
2. 过滤过小 coast（如 <2°）勿更新 k，避免把「几乎到点才切」的样本污染 EMA。  
3. **P1**：完整 R-stall90 / R-earlyRefine 可见策略包。  
4. P2/P4：bias 分流固化 + capture 窗。

---

*文档生成：2026-07-23；实验节更新于同日 coast 修后复测；停点同步 P0 ✅（err≈1.8–9.6° / …105128.txt）。*

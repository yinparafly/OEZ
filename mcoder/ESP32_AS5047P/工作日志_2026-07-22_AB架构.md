# 工作日志 · 2026-07-22 · 架构 A/B 落地

台架：ESP32-S3 + AS5047P + ESC；COM10 烧录自检。**未 git commit/push。**

## 已实现 A/B

| 项 | 结果 |
|----|------|
| 术语 | 采集2k / 使用2k / 输出400；「取用」作废 |
| BLE ON/OFF/?、FLIGHT ON/OFF/? | ✅；FLIGHT ON 关 BLE + 禁 HOST_TIMEOUT |
| 输出 400Hz | esp_timer→信号量→`motorControlTask` Core1；`CTRL?` meas_out≈400 |
| 采集/使用 2k | `ENC?` meas_hz≈2000 |
| ESC 默认 | FREQ 400 |
| USB 遥测 | **20Hz**（每秒 20 帧完整 CSV）；`TELEM?` |
| 实时路径禁打印 | 采集回调 Enter/Exit + 控制任务 Forbid → 异步日志环；`telemTask`/`loop` 排出 |
| 环缓/Q16 | 未做（阶段 D/E）；留注释 |

## 自检摘要（`pc/_ab_selfcheck.py`）

- PING / ENC? / CTRL? / TELEM? / BLE? / CORE? OK
- enc_sample_hz≈2000；ctrl_out≈400；usb_telem_hz=20；CORE ctrl=1 enc_cb=0
- 短时 CLOSED+RPM2000：4s 内 rpm 未起来（soft 斜坡/脉宽爬至~1255）；**频率链路已验**；长测待做
- 收尾 ESTOP/STOP/pulse=1000；测试态停保活后出现 `host_timeout` ESTOP 符合预期

## 文件

- `firmware/ESP32ReadAS5047P/ESP32ReadAS5047P.ino`、`ble_host.*`
- 总纲 `ESP32_AS5047P_转速采样与控制架构需求.md`（术语+遥测20Hz+A/B勾选）
- `pc/_ab_selfcheck.py`

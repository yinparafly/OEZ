---
description: Bench/motor tests — run without asking; 3 min silence = go
alwaysApply: true
---

# 台架实测：默认直接跑

对本目录电机/编码器/ESC 相关实测与自动脚本：

1. **不要再问「要不要跑」**——方案/脚本就绪且安全条件满足时直接执行。
2. 若需要用户拍板但对方未答：**约 3 分钟无答复则按已定方案自动开跑**。
3. 仍遵守既有安全：单 COM 锁、FAIL→FREQ 50 + 1000 μs、空载标注；不并行占 COM10。
4. 跑完直接给结论与报告路径，不必等二次确认再写冻结。

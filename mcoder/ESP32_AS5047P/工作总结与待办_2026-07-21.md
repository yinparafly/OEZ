# 工作总结与待办事宜 · 2026-07-21（上午）

承接 07-19 空载闭环冻结与 BLE 简控；本日主线为 **扑翼霍尔相位 + 实测减速比**。

---

## 1. 工作总结

1. **霍尔接入**：GPIO4 下扑 / GPIO5 上举；YL-57(A3144) **VCC=5V**，触发按厂商 51 样例为**低有效**；DO→ESP32 须电平处理。  
2. **实测显示**：电机编码器 RPM + 霍尔周期输出 Hz；减速比实测对照设计 `(59×79)/(12×14)`。  
3. **BLE 显示修复**：TX 特征值被 ACK/HALL 冲掉导致手机转速异常 → 已修；手机 v1.0.6。  
4. **文档**：`扑翼霍尔接线_2026-07-21.md`、本日志。

### 冻结提醒（空载控制，未改）

```text
PWM @ 400 Hz，Ka=0.05，ADG OFF，SOFT UP800/DOWN300，ctrl 250Hz
BLE：OEZ-RPM / NUS，须 PING
```

---

## 2. 待办事宜

| 优先级 | 事项 | 说明 |
|--------|------|------|
| 高 | 霍尔接线后 **≥2 次下扑** 实测 out_Hz / gear_meas，记录偏差% | 确认与设计 27.744 是否一致 |
| 高 | 上举霍尔相对 0° 的安装偏角校准 | 用 `disc_est` / 磁编码反算 |
| 高 | 接扑翼后 **flap 重学**（前馈 + GainMap） | 空载表不可直接搬 |
| 中 | BLE 距离/障碍实测（PC + 手机） | 丢包与可用距离 |
| 中 | DO 分压/电平转换落板确认 | 防 5V 灌 GPIO |
| 中 | 扑翼台架 **T3b**（平均稳 vs 相位纹波） | |
| 低 | 手机 Release 签名包 | 现 debug APK |
| 低 | Notify 在 Android 再摸底 | 现 READ 轮询为主 |

### 已完成（本日上午）

- [x] 霍尔 GPIO4/5 固件与 PC/手机 UI  
- [x] 模块资料入库说明（VCC=5V、低有效）  
- [x] BLE 遥测显示修复 + 实测输出频率/减速比对照  
- [x] COM10 烧录当前固件  

---

*详见 `工作日志_2026-07-21.md`。*
